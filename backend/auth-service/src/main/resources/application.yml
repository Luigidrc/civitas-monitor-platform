server:
  port: 8082

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://localhost:5432/civitas_auth_db
    username: admin
    password: password123
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true

security:
  jwt:
    secret: "mySuperSecretKeyForJWTSigning12345"
    expirationMinutes: 120
